# Branch Schema

The branch schema models derived or declared branch metadata such as retries,
fallbacks, repairs, and subagents.

Schema file:

- [`schemas/v1/branch.schema.json`](../../schemas/v1/branch.schema.json)
