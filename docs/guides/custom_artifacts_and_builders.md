# Custom Artifacts and Builders

ClawGraph v1 does not require a complex DSL.

Instead it should support:

- typed artifact records
- structured selection queries
- pluggable builder implementations

Good extension points:

- new artifact families
- new selection filters
- custom dataset builders
- custom export manifests
