# Examples

This directory will host reference integrations for:

- transparent OpenClaw proxy mode
- proxy plus context headers
- proxy plus semantic contract
- dataset export to async RL and distillation stacks
