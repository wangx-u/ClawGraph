# Artifact Schema

The artifact schema defines external supervision attached to facts, runs,
sessions, or branches.

Schema file:

- [`schemas/v1/artifact.schema.json`](../../schemas/v1/artifact.schema.json)
